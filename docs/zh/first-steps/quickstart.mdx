---
title: Quickstart
description: "Stagehand 让你用自然语言和代码构建网页自动化。"
---

如果这是你**首次使用 Stagehand**，建议先试试 [Director](https://director.ai)。它是一个代理，能让你用自然语言搭建 Stagehand 工作流。你也可以通过我们的 [MCP 服务器](/zh/integrations/mcp/introduction) 体验 Stagehand。

否则，最快的入门方式是使用我们的 CLI。它会基于合理的默认配置和示例脚本，脚手架一个可直接运行的 Stagehand 应用。

<Note>
  本快速入门适用于 **TypeScript**。如需 **Python**，请参见[安装指南](/zh/first-steps/installation)。
</Note>

## 1) 创建一个示例项目

<CodeGroup>
  ```bash Bash
  npx create-browser-app
  ```
</CodeGroup>

## 2) 运行

根据 CLI 提示进入项目目录并添加你的 API 密钥。然后运行示例脚本。

<CodeGroup>
  ```bash Bash
  cd my-stagehand-app # 进入项目目录
  cp .env.example .env  # 添加你的 API 密钥
  npm start # 运行示例脚本
  ```
</CodeGroup>

## 3) 使用 Stagehand（act、extract、observe）

该脚手架包含一个 index.ts 文件，内含示例脚本。如下所示：

<CodeGroup>
  ```typescript TypeScript
  import "dotenv/config";
  import { Stagehand } from "@browserbasehq/stagehand";

  async function main() {
    const stagehand = new Stagehand({
      env: "BROWSERBASE"
    });

    await stagehand.init();

    console.log(`Stagehand Session Started`);
    console.log(`Watch live: https://browserbase.com/sessions/${stagehand.browserbaseSessionID}`);

    const page = stagehand.page;

    await page.goto("https://stagehand.dev");

    const extractResult = await page.extract("Extract the value proposition from the page.");
    console.log(`Extract result:\n`, extractResult);

    const actResult = await page.act("Click the 'Evals' button.");
    console.log(`Act result:\n`, actResult);

    const observeResult = await page.observe("What can I click on this page?");
    console.log(`Observe result:\n`, observeResult);

    const agent = await stagehand.agent({
      instructions: "You're a helpful assistant that can control a web browser.",
    });

    const agentResult = await agent.execute("What is the most accurate model to use in Stagehand?");
    console.log(`Agent result:\n`, agentResult);

    await stagehand.close();
  }

  main().catch((err) => {
    console.error(err);
    process.exit(1);
  });

  ```
</CodeGroup>

<Tip>
  使用前，请在 `.env` 中设置各模型提供商的密钥（如 `OPENAI_API_KEY`）。如需使用云浏览器，请添加 `BROWSERBASE_API_KEY` 和 `BROWSERBASE_PROJECT_ID`。
</Tip>

## 后续步骤

了解 Stagehand 的基础原语：act、extract、observe 和 agent。

<CardGroup cols={2}>
  <Card title="Act" icon="arrow-pointer" href="/zh/basics/act">
    以自然语言在网页上执行操作
  </Card>

  <Card title="Extract" icon="download" href="/zh/basics/extract">
    通过 Zod 模式提取结构化数据
  </Card>

  <Card title="Observe" icon="eye" href="/zh/basics/observe">
    发现可执行的元素与动作
  </Card>

  <Card title="Agent" icon="robot" href="/zh/basics/agent">
    自主执行的多步浏览器工作流
  </Card>
</CardGroup>
