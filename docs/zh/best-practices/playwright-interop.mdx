---
title: "Playwright 互操作性"
description: "Stagehand 如何与 Playwright 协同工作"
---

Stagehand 构建在 [Playwright](https://playwright.dev/) 之上，因此你可以通过 Stagehand 实例直接调用 Playwright 的方法。

## `page` 和 `context`

`stagehand.page` 和 `stagehand.context` 分别是 Playwright 的 `Page` 与 `BrowserContext` 实例。使用它们与 Stagehand 所使用的 Playwright 实例进行交互。

<CodeGroup>
```TypeScript TypeScript
const page = stagehand.page;
// 原生 Playwright 方法可用
await page.goto("https://github.com/browserbase/stagehand");

// Stagehand 对 Playwright 对象进行了增强/重写
await page.act("click on the contributors")
```

```python Python
page = stagehand.page
# 原生 Playwright 方法可用
await page.goto("https://github.com/browserbase/stagehand")

# Stagehand 对 Playwright 对象进行了增强/重写
await page.act("click on the contributors")
```
</CodeGroup>

## Stagehand 与 Playwright 对比
下面演示如何分别使用 Stagehand 和 Playwright 从 AI Grant 网站提取公司列表。

<img src="/images/stagehand-playwright.png" alt="Stagehand v. Playwright" />

上述 Stagehand 示例可轻松复用以从其他网站提取数据，而 Playwright 示例则需要针对每个新网站重新编写。